pipeline {
    agent {
        label 'slave-node'
    }
    stages{
        stage('🌐 Install Nginx') {
            steps {
                echo '🌐 Installing Nginx...'
            }
        }
    }
}
